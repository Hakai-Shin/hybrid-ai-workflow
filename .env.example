# Copy to .env and fill in real values — never commit .env
GCP_PROJECT_ID=your-project-id
GCS_ARTIFACT_BUCKET=docubrain-artifacts-dev
PUBSUB_TOPIC=artifact-intake
GOOGLE_APPLICATION_CREDENTIALS=/secrets/enclave-sa.json
PRESIDIO_URL=http://presidio-sidecar:8080
ENCLAVE_VERSION=0.1.0
DATA_DIR=/data
